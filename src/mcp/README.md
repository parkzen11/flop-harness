# mcp

A Model Context Protocol server exposing the harness to MCP clients (keys, offers, spec,
deal.take, verdict, refold). Placeholder: built as units `u-mcp-01` and `u-mcp-02`. The
official `tclk-mcp` covers frames only; this server covers the loops.
