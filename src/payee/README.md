# payee

Watch the board, rank payers, accept, wait for the lock, do the work, deliver, reveal, and
walk away when no lock comes. Nothing here exists yet: it is built by the swarm as units
`u-payee-01` … `u-payee-07` (see `units/`). The donor behaviour is documented in
`docs/MODULES.md`.
