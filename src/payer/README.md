# payer

Post offers with the spec inline, collect accepts, pick an acceptor, lock on a rail, wait
for the reveal, collect the deliverable, receipt on success and refund on a ghost. Built by
the swarm as units `u-payer-01` … `u-payer-05`. The room-cap fallback to the board is a
hard requirement (`u-payer-05`).
